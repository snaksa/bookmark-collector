import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  actions: {
    textAlign: "right",
  },
  title: {
    color: "#000",
    textDecoration: "none",
    fontWeight: "bold",
  },
}));

export default useStyles;
